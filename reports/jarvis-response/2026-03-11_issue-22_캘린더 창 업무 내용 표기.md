# 🤵 자비스 개발팀 — 이슈 이해보고서

## 기본 정보
| 항목 | 내용 |
|------|------|
| 이슈 번호 | #22 |
| 제목 | 캘린더 창 업무 내용 표기 |
| 김감사 심각도 | MAJOR |
| 김감사 Overall Score | 83.5/100 |

## QA 보고서 핵심 요약
1. **기능 정상, UI 치명적 결함**: 업무가 캘린더에 날짜별로 정상 등록되지만, 단순 선(line)으로만 표시되어 업무 식별 불가
2. **UX 점수 70점**: 정보 가독성 심각 저하로 캘린더 기능이 실질적으로 사용 불가능한 상태
3. **즉시 조치 필요**: 2주 내 수정 권장, 핵심 기능의 실효성 상실로 HIGH 우선순위 판정

## 원인 추정
캘린더 렌더링 로직에서 이벤트 표시 부분의 구현 누락 또는 오류로 추정됩니다.

- **파일**: `frontend/calendar.html` (추정)
- **함수**: 이벤트 렌더링 관련 JavaScript 함수 (추정: `renderCalendarEvents()` 또는 FullCalendar 설정)
- **원인**:
  1. 캘린더 라이브러리 초기화 시 `eventContent` 또는 `eventRender` 콜백이 제대로 구현되지 않음
  2. CSS에서 이벤트 텍스트가 `display: none` 또는 배경색과 동일한 색상으로 설정되어 있을 가능성
  3. 백엔드에서 전달되는 이벤트 데이터 구조는 정상이나, 프론트엔드에서 `title` 속성을 렌더링하지 않음
  4. 이벤트 클릭/호버 시 상세 정보를 표시하는 모달 로직 미구현

## 영향 범위
- **직접 영향**: 캘린더 페이지 전체 (모든 사용자)
- **간접 영향**:
  - 업무 필터링/검색 기능과의 연동 필요
  - 일정 기반 알림 기능 사용 시 사용자가 알림 내용을 캘린더에서 확인할 수 없음
  - 협업 기능(업무 공유, 팀 일정 보기) 활용도 저하
- **사용자 경험**: 대체 수단(리스트 뷰)에 의존해야 하며, 타 협업 툴 대비 경쟁력 크게 저하

## 수정 계획
1. **캘린더 이벤트 렌더링 로직 수정** (필수)
   - FullCalendar 설정에서 `eventContent` 콜백 구현
   - 업무 제목 표시 (20자 이내 요약, 초과 시 ellipsis 처리)
   - 업무 카테고리별 색상 구분 적용

2. **상세 정보 표시 기능 추가** (단계적)
   - Phase 1: 이벤트 클릭 시 모달창으로 업무 상세 정보 표시
   - Phase 2: 호버 시 툴팁으로 간단 정보 제공 (업무명, 담당자, 마감일)

3. **CSS 스타일링 점검**
   - 이벤트 텍스트 가시성 확인 및 수정
   - 반응형 디자인 대응 (1280x800 등 다양한 해상도)

4. **구체적 코드 수정 예시**:
   ```javascript
   // calendar.html 내 FullCalendar 초기화 부분
   const calendar = new FullCalendar.Calendar(calendarEl, {
     // ... 기존 설정
     eventContent: function(arg) {
       return {
         html: `<div class="fc-event-title">${arg.event.title}</div>`
       };
     },
     eventClick: function(info) {
       showTaskModal(info.event.id); // 모달 표시 함수
     }
   });
   ```

## 예상 작업량
**중** — 2~3일 소요 예상

**이유**:
- 캘린더 라이브러리 설정 수정 및 테스트: 0.5일
- 모달 컴포넌트 구현 및 데이터 연동: 1일
- 스타일링 및 반응형 대응: 0.5일
- QA 및 버그 수정: 1일
- 단순 렌더링 로직 수정이지만, 모달 연동 및 다양한 시나리오 테스트 필요

## 우선순위 의견
**즉시** — 다음 스프린트 시작 전 착수

**이유**:
- 김감사 판정: HIGH 우선순위, 2주 내 수정 권장
- 캘린더는 업무 관리 시스템의 핵심 기능으로, 현재 "존재하지만 쓸 수 없는 상태"
- 사용자가 제보할 정도로 불편함이 가시화된 상황
- 타 협업 툴(Notion, Asana) 대비 경쟁력 저하 요인
- 작업량이 중간 수준으로 빠른 수정 가능
- 추가 기능 개발(Phase 2, 3)은 백로그에 등록하되, Phase 1은 즉시 착수 권장

## 대표님 확인 요청
위 수정 계획을 승인해 주시면 즉시 작업에 착수하겠습니다.

- **단기 목표** (즉시): 업무 제목 표시 + 클릭 시 모달 상세 보기
- **중기 목표** (다음 스프린트): 호버 툴팁, 카테고리별 색상, 드래그 앤 드롭
- **장기 목표** (백로그): 월/주/일 뷰 전환, 구글 캘린더 동기화

승인 시 예상 완료일: 2026-03-18 (1주일 이내)