# ⏱️ Task: 업무 트래킹 및 시각화 MVP 구현

## 1. 기반 인프라 구축
- [ ] `setup_structure.gs` 수정: `Tasks` 시트에 `StartTime`, `EndTime`, `DurationMin` 컬럼 정의 추가
- [ ] "시트 구조 자동 생성" 실행하여 실제 시트에 컬럼 반영

## 2. 백엔드 타임스탬프 로직 구현
- [ ] `web_app.gs` 내 `changeTaskStatusFromWeb` 함수 수정
  - [ ] `진행중` 전환 시 `StartTime` 기록 로직 추가
  - [ ] `완료` 전환 시 `EndTime` 기록 및 `DurationMin` 계산 로직 추가

## 3. 프론트엔드 데이터 연동 (관리자 한정)
- [ ] `web_app.gs` 내 `getMyTasksForWeb` 함수 수정 (트래킹 데이터 필드 추가 반환)
- [ ] `judy_workspace.html`에 관리자 로직 추가 (`송용남`, `정혜림` 여부 확인)
- [ ] 관리자인 경우에만 업무 리스트 템플릿에 `소요 시간` 뱃지 UI 노출

## 4. 대시보드 시각화 강화 (관리자 한정)
- [ ] 관리자인 경우 상단 요약 카드 영역에 `오늘 총 몰입 시간` 계산 로직 및 UI 추가

## 5. 실전 테스트 (시나리오 실행)
- [ ] "Email 자동화 기능 개발" 업무를 통한 시작~완료 라이프사이클 테스트
- [ ] 시간 계산 및 대시보드 합산 시간 정확도 검증
